config:
  target: 'http://localhost:4000'  
  phases:
    - duration: 1
      arrivalRate: 1

scenarios:
  - flow:
      - get:
          url: "/api/storefront/media/readRecords?fields=%7B%7D&uniqueFlag=true"  
          headers:
            If-None-Match: ""  